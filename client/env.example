# Copy to .env.local for local overrides (optional).
# On Vercel: add VITE_API_URL in Project → Settings → Environment Variables.

# Your deployed API base URL (no trailing slash). Required for production builds on Vercel.
# VITE_API_URL=https://your-api.onrender.com
